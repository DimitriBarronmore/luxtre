#!/usr/bin/env luajit
-- print(arg[0])

package.path = arg[0]:sub(1, -8) .. "?.lua"
-- local loader = require(arg[0] .. "/loader")
local loader = require("luxtre.loader")
local file = arg[1]

local res, err = pcall(loader.dofile, file)
if err then
    error("lux: " .. err, 0)
end
